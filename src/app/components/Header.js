
import React from "react";
import PropTypes from "prop-types";

export default function Header(props) { 
        return (
            <div> 
            <h2>Header</h2>
            </div>
        )
} 


Header.defaultProps = {
    
}

Header.propTypes = {
    
}